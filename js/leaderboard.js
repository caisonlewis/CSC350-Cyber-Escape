/*
*****Leaderboard Class*****
* Date: 2/18/24
* Dev: Victoria Saunders
*
* This class extends User class. Sorts User's by
* their total points. Prints the users in order
* from highest to lowest total points.
*/

/*
class LBUser {

    constructor

}

class Leaderboard {

    //JS has a built in sort function

    this.sorted_points = (User.total_points).sort();

//for loop iterates and prints the users in order

for (let i = 0; i < sorted_points.length; i++) {

    console.log(i + 1 + ". " + "\t " + User.username + "\t" + User.total_points);

}
}
*/
